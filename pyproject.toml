[tool.poetry]
name = "telegram bot for chatgpt"
version = "0.1.0"
description = "telegram bot for using chatgpt"
authors = ["sirlaurie <liusuzhang@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
python-telegram-bot = "^20.0"
httpx = "^0.23.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
